extends layouts/main

block content 
    .form-container 
        form(action=routes.login, method="post")
            input(type="email", name="email", placeholder="Email", requred=true)
            input(type="password", name="password", placeholder="Password", requred=true)
            input(type="submit", value="Log in") 
        include partials/socialLogin